<h1>Files#list</h1>
<% for file in @files %>
  <% if file == '.' or file == '..'
       next 
     end %>
  <%= file %></br>
<% end %>
<p><%= link_to "Logout", logout_path, :confirm => 'Are you sure?', :method => :delete%></p>
